<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Reset Code</title>
    <link rel="stylesheet" href="{{ asset('Back/css/vertical-layout-light/style.css') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #e7f9e7; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .container { max-width: 500px; width: 100%; }
        .card { padding: 40px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); }
        .btn-primary { background-color: #66bb6a; border-color: #66bb6a; }
        .btn-primary:hover { background-color: #81c784; border-color: #81c784; }
        .text-green { color: #66bb6a; }
        .brand-logo { display: block; margin: 0 auto 20px; width: 80px; }
    </style>
</head>
<body>
<div class="container">
    <div class="card p-4 shadow-sm">
        <div class="text-center">
            <img src="{{ asset('Back/images/favicon11.png') }}" alt="logo" class="brand-logo">
        </div>
        <h2 class="text-center text-green">Verify Reset Code</h2>
        <p class="text-center text-green mb-4">Enter the verification code sent to your email.</p>
        {% for flash in app.flashes('danger') %}
            <div class="alert alert-danger">{{ flash }}</div>
        {% endfor %}
        {{ form_start(form, {'action': path('app_verify_reset_code')}) }}
            <div class="form-group">
                {{ form_row(form.verification_code, {'attr': {'class': 'form-control', 'placeholder': 'Enter 6-digit code'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.verify, {'attr': {'class': 'btn btn-primary btn-block mt-3'}}) }}
            </div>
        {{ form_end(form) }}
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
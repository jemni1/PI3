<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facture</title>
    
    <style>
        .invoice {
            max-width: 800px;
            margin: auto;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .invoice h2 {
            font-size: 28px;
        }

        .invoice h4 {
            font-size: 20px;
        }

        .invoice p {
            font-size: 16px;
            margin: 5px 0;
        }

        .invoice .row {
            margin-bottom: 15px;
        }

        .invoice hr {
            border-top: 1px solid #ddd;
        }

        .text-success {
            color: #28a745;
        }

        .text-right {
            text-align: right;
        }

        .btn {
            font-size: 14px;
            padding: 10px 15px;
            border-radius: 4px;
            color: #fff;
            background-color: #28a745;
            border: none;
            transition: all 0.3s ease;
        }

        .btn:hover {
            opacity: 0.9;
        }
    </style>
    </head>
<body>
    <div class="container mt-5">
        <div class="invoice p-5">
            <div class="row">
                <div class="col-md-6">
                    <h2 class="text-success">Facture</h2>
                    <p><strong>Numéro de la commande:</strong> {{ commande.id }}</p>
                    <p><strong>Date:</strong> {{ commande.date|date('d-m-Y H:i') }}</p>
                </div>
                <div class="col-md-6 text-right">
                    <h4 class="text-success">Détails de la commande</h4>
                    <p><strong>Produit:</strong> {{ commande.nom }}</p>
                    <p><strong>Quantité:</strong> {{ commande.quantite }}</p>
                    <p><strong>Prix total:</strong> {{ commande.prix }} €</p>
                </div>
            </div>
            <hr>

            <div class="row">
                <div class="col-md-12">
                    <h4 class="text-success">Informations sur le client</h4>
                    <p><strong>Nom du client:</strong> {{ commande.nom }}</p>
                    <p><strong>Adresse de livraison:</strong> {{ commande.prix }}</p>
                </div>
            </div>
            <hr>

            <div class="row">
                <div class="col-md-12">
                    <p class="text-center">
                        <a href="{{ path('facture_show', {'id': commande.id}) }}" class="btn btn-success">Imprimer la facture</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
    </body>
</html>



<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facture</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            color: #1a472a;
            background: #f0f9f0;
            padding: 20px;
            margin: 0;
        }

        .invoice {
            max-width: 800px;
            margin: auto;
            padding: 40px;
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(46, 125, 50, 0.1);
            position: relative;
            overflow: hidden;
        }

        .background-logo {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            opacity: 0.05;
            width: 60%;
            height: auto;
            z-index: 0;
        }

        .logo-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .logo {
            max-width: 150px;
            height: auto;
        }

        h2 {
            color: #2e7d32;
            font-size: 2.2em;
            margin: 20px 0;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        h4 {
            color: #2e7d32;
            font-size: 1.3em;
            margin: 20px 0 15px;
            padding-bottom: 8px;
            border-bottom: 2px solid #c8e6c9;
            position: relative;
            z-index: 1;
        }

        .detail-box {
            background: #f5f9f5;
            padding: 20px;
            border-radius: 12px;
            margin: 15px 0;
            border-left: 4px solid #2e7d32;
            position: relative;
            z-index: 1;
        }

        p {
            margin: 10px 0;
            font-size: 16px;
            line-height: 1.5;
        }

        strong {
            color: #2e7d32;
            font-weight: 600;
        }

        .price-info {
            font-size: 24px;
            font-weight: bold;
            color: #2e7d32;
            text-align: right;
            padding: 15px 20px;
            background: #f5f9f5;
            border-radius: 10px;
            margin-top: 20px;
        }

        hr {
            border: none;
            height: 2px;
            background: #c8e6c9;
            margin: 25px 0;
        }

        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #c8e6c9;
            position: relative;
            z-index: 1;
        }

        .footer p {
            color: #2e7d32;
            font-size: 16px;
            margin: 10px 0;
        }

        @media print {
            body {
                background: white;
                padding: 0;
            }

            .invoice {
                box-shadow: none;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="invoice">
        <!-- Logo en filigrane -->
        <img src="{{ asset('Back/images/DataFarm.png') }}" alt="Logo en filigrane" class="background-logo">
        
        <!-- Logo dans l'en-tête -->
        <div class="logo-header">
            <img src="{{ asset('Back/images/DataFarm.png') }}" alt="Logo DataFarm" class="logo">
            <p><strong>Facture #{{ commande.id }}</strong></p>
        </div>

        <h2>Facture</h2>

        <div class="detail-box">
            <p><strong>Numéro de commande:</strong> #{{ commande.id }}</p>
            <p><strong>Date:</strong> {{ commande.date|date('d-m-Y H:i') }}</p>
        </div>

        <h4>Détails de la commande</h4>
        <div class="detail-box">
            <p><strong>Produit:</strong> {{ commande.nom }}</p>
            <p><strong>Quantité:</strong> {{ commande.quantite }}</p>
            <div class="price-info">
                <strong>Total:</strong> {{ commande.prix }} €
            </div>
        </div>

        <h4>Client</h4>
        <div class="detail-box">
            <p><strong>Nom:</strong> {{ commande.nom }}</p>
        </div>

        <div class="footer">
            <p>🌿 Merci de votre confiance ! 🌿</p>
            <p>DataFarm - Votre partenaire de confiance</p>
        </div>
    </div>
</body>
</html>